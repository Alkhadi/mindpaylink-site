# Cloudflare R2 (replace the placeholders and then rename to .env)
R2_ACCOUNT_ID=1afe4e79bf9f771faa579b2cba294892
R2_BUCKET=mshare-assets
R2_ACCESS_KEY_ID=YOUR_R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY=YOUR_R2_SECRET
# Optional: public base (if you have a public bucket or custom domain)
# If omitted, scripts will use: https://${R2_BUCKET}.${R2_ACCOUNT_ID}.r2.cloudflarestorage.com
R2_PUBLIC_BASE=
# Optional: prefix for object keys (e.g. "site/"). Leave empty to keep local relative paths.
R2_UPLOAD_PREFIX=
